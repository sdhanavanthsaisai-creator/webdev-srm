/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Express handlers are compiled by Vercel/Next automatically; nothing exotic needed.
};

export default nextConfig;
